#include "../pch.h"

namespace Photon::Extension{
    //所有效果器的基类
    class EffectBase{
        public:
            //构造函数
            EffectBase();
            //析构函数
            virtual ~EffectBase();
    };
}