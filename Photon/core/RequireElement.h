#include "../pch.h"
#include "../core/"
namespace Photon::Core{
    class RequireElement{
        public:
            //构造函数
            RequireElement();
            //析构函数
            virtual ~RequireElement();
            
            
}